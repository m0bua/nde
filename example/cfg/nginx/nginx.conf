user www-data;
worker_processes auto;
pid /run/nginx.pid;

events {
  worker_connections 1024;
}

http {
  resolver 127.0.0.11;

  sendfile on;
  keepalive_timeout 65;

  types_hash_max_size 2048;

  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  ssl_protocols TLSv1 TLSv1.1 TLSv1.2; # Dropping SSLv3, ref: POODLE
  ssl_prefer_server_ciphers on;

  server {
    server_name _;

    listen 443 ssl http2;
    ssl_certificate /etc/nginx/cert/nginx-selfsigned.crt;
    ssl_certificate_key /etc/nginx/cert/nginx-selfsigned.key;

    return 301 http://$host$request_uri;
  }

  server {
    server_name _ ~^(?:www\.)?(?<domain>[^.]+)\.(?<end>[^.]+)$;
    listen 80;

    client_max_body_size 100m;

    error_page 418 = @mail;
    if ($domain = mail) { return 418; }
    if ($domain = m) { return 418; }

    set $php php73;

    set $basepath /var/www;
    if ($domain ~* ^.+$){set $path $basepath/$domain;}

    if (!-d $path){set $path $basepath/main;}
    if (!-d $path){set $path $basepath/v;}
    if ($end ~* (\d+)) {set $php php$1;}
    if (-d $path/web){set $path $path/web;}

    root $path;
    index index.php index.htm index.html;

    location / {
      # add_header content-type "text/html";
      # return 200 "$path - $domain - $end - $php";
      add_header Last-Modified $date_gmt;
      add_header Cache-Control 'no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0';
      if_modified_since off;
      expires off;
      etag off;

      try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
      try_files $uri =404;
      include fastcgi_params;
      fastcgi_pass ${php}:9000;
      fastcgi_keep_conn on;
      fastcgi_index index.php;
      fastcgi_split_path_info ^(.+\.php)(/.+)$;
      fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
      fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location @mail{
      proxy_pass http://mail:8025;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection 'upgrade';
      proxy_set_header Host $host;
      proxy_cache_bypass $http_upgrade;
    }
  }
}

#stream {
#  map $ssl_preread_server_name $name {
#    mysql5.d mysql5;
#    mysql8.d mysql8;
#    default mysql5;
#  }
#  upstream mysql5 {server mysql5:3306;}
#  upstream mysql8 {server mysql8:3306;}
#  #upstream maria {server maria:3306;}
#  server {
#    listen 3306;
#    proxy_pass $name;
#  }
#}